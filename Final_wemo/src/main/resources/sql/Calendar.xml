<?xml version="1.0" encoding="UTF-8"?>
<!--  SQL 맵퍼 파일은 xml이기 때문에 제일 먼저 xml 선언이 옵니다. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Cal">

	<insert id="insert" parameterType="calendar">
		insert into calendar
		values(#{USER_EMAIL},#{MEMO_SUB},sysdate,sysdate,#{MEMO_PRE})
	</insert>
	
	<select id="select">
		select * from calendar
			where MEMO_SUB AND USER_EMAIL=#{USER_EMAIL}
	</select>
	
	<update id="update" parameterType="calendar">
		update calendar
			set	MEMO_SUB = #{MEMO_SUB},
				MEMO_TEX = #{MEMO_TEX},
				MEMO_DATE = #{MEMO_DATE},
				MEMO_PRE = #{MEMO_PRE}
			where USER_EMAIL = #{USER_EMAIL}
	</update>
	
	<delete id="delete" parameterType="calendar">
		delete from calendar
			where USER_EMAIL = #{USER_EMAIL}
	</delete>
	
</mapper>